<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/widget_bg"
    android:gravity="center_vertical">

    <!-- Header: heart emoji + "From [partner]" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_heart"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_heart_emoji"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/widget_sender"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="From Your Love"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#FF8FAB"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Divider line (ImageView because RemoteViews does not support plain View) -->
    <ImageView
        android:id="@+id/widget_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="12dp"
        android:src="@drawable/widget_divider"
        android:scaleType="fitXY"
        android:contentDescription="@null" />

    <!-- Message photo (hidden by default, shown when image exists) -->
    <ImageView
        android:id="@+id/widget_image"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:contentDescription="@null"
        android:clipToOutline="true" />

    <!-- Message content -->
    <TextView
        android:id="@+id/widget_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No messages yet"
        android:textSize="15sp"
        android:textColor="#2D2D2D"
        android:maxLines="3"
        android:ellipsize="end"
        android:lineSpacingMultiplier="1.3"
        android:fontFamily="serif" />

    <!-- Timestamp (right-aligned) -->
    <TextView
        android:id="@+id/widget_timestamp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="end"
        android:textSize="10sp"
        android:textColor="#FF8FAB"
        android:maxLines="1"
        android:visibility="gone" />

</LinearLayout>
